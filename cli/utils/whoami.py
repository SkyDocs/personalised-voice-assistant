from cli.utils import user, bot

def main(user):
	if user == 0:
		return("I am the general user. I am not giving the presonalised voice assistant.")
		return("To activate the presonalised voice assistant say RECOGNISE")

	elif user == 1:
		return("You the registered User 0!")
		return("Welcome back!")

	elif user == 2:
		return("You the registered User 1!")
		return("Welcome back!")

	elif user == 3:
		return("You the registered User 2!")
		return("Welcome back!")
